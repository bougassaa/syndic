{% use 'bootstrap_5_layout.html.twig' %}

{% block _proprietaire_possessions_entry_row %}
    <div class="card mb-3">
        <div class="card-body pb-0">
            {{ form_widget(form) }}
        </div>
    </div>
{% endblock %}