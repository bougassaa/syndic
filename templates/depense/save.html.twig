{% extends 'base.html.twig' %}

{% block title %}
    {{ ('depense.'~mode)|trans }}
{% endblock %}

{% block body %}
    <div class="d-flex align-items-center flex-wrap gap-2 mb-2">
        <h2 class="flex-fill m-0">
            {{ ('depense.'~mode)|trans }}
        </h2>
        <a class="text-sm" href="{{ path('app_type_depense_new') }}">
            {{ 'type-depense.new'|trans }}
        </a>
    </div>
    {{ form(form) }}
    <input type="hidden" id="typesDepenseMontant" value="{{ types|json_encode }}">
{% endblock %}
