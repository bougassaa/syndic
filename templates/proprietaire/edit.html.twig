{% extends 'base.html.twig' %}

{% block title %}
    {{ 'proprietaire.edit'|trans }}
{% endblock %}

{% block body %}
    <h2>{{ 'proprietaire.edit'|trans }}</h2>
    {{ form_start(form) }}
        {{ form_row(form.nom) }}
        {{ form_row(form.prenom) }}
        {{ form_row(form.possessions) }}
        {{ form_rest(form) }}
    {{ form_end(form) }}
{% endblock %}
